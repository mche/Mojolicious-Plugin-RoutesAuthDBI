% title 'Не найдено';

<h1>Страница  "<%= req->url->path %>" не найдена [<%= $status %>] </h1>
%# <div style="white-space: pre;"><%#= dumper req->headers %></div>
%# app->log->debug(dumper $self->req->headers);
<div style="white-space: pre;"><%= dumper $self->req %></div>
%# app->log->debug(dumper req);
